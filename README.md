# serviceMocking
​A mock service is a fake stand-in for a real server. When your application tries to talk to the real service (using a network protocol like REST/JSON), it talks to the mock instead. The mock doesn't execute any "business logic" (it doesn't process the payment or look up data); it just returns a "canned response"—a pre-recorded block of data
